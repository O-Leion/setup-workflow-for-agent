# setup-workflow-4-RooCode

🌐 [English Version](./README_EN.md)

**AI Agent Workspace Scaffolder for Roo Code**

Roo Code（VS Code AI拡張機能）において、AIエージェント開発に最適化されたワークスペース環境を自動構築するためのワークフローです。
`.roo/` フォルダにワークスペース共通ルールとモード別ルール（Code, Architect, Ask, Debug）を集約した高度な構成を、対話形式で安全にセットアップします。

## 🚀 特徴 (Features)

* **対話型セットアップ**: AIが現状を確認し、ユーザーの意図を汲みながらセットアップを進めます。
* **モード別ルール管理**: Roo Codeの4つのモード（Code, Architect, Ask, Debug）それぞれに専用ルールを設定可能。
* **ルール階層構造**:
    * `.roo/rules/`: 全モード共通ルール
    * `.roo/rules-code/`: Codeモード固有ルール
    * `.roo/rules-architect/`: Architectモード固有ルール
    * `.roo/rules-ask/`: Askモード固有ルール
    * `.roo/rules-debug/`: Debugモード固有ルール
* **カスタムモード対応**: `.roomodes` でプロジェクト固有のカスタムモードを定義可能。
* **ディレクトリ構造化**: `docs`, `src`, `tests` の分離を自動化。

## 📦 インストール方法 (Installation)

### 1. 使用方法

> ⚠️ Roo Code自体にはGlobal Workflowsの仕組みはありません。このワークフローファイルを新規プロジェクトのルートに配置し、Roo CodeのAIに読み込ませて使用してください。

### 2. 手順
1. 新規プロジェクト（空のフォルダ）をVS Codeで開きます。
2. `setup-assistant.md` をプロジェクトルートに配置します。
3. Roo Codeのチャットで以下のように指示してください。

> **「`setup-assistant.md` を読んでセットアップを実行してください」**

4. セットアップ完了後、`setup-assistant.md` は削除して構いません。

## 📂 生成される構成 (Directory Structure)

```text
.
├── .roo/                     <-- [AI設定] (隠しフォルダ)
│   ├── rules/                <-- 全モード共通ルール
│   │   ├── 01-coding-standards.md
│   │   └── 02-project-conventions.md
│   ├── rules-code/           <-- Codeモード固有ルール
│   ├── rules-architect/      <-- Architectモード固有ルール
│   ├── rules-ask/            <-- Askモード固有ルール
│   └── rules-debug/          <-- Debugモード固有ルール
├── .roomodes                 <-- [カスタムモード定義]
├── docs/                     <-- [ドキュメント]
│   └── design/
├── src/                      <-- [ソースコード]
├── tests/                    <-- [テストコード]
├── scratchpad.md             <-- [AI作業ログ]
└── .gitignore                <-- [Git設定]
```

## ⚠️ 隠しフォルダ (.roo) が見えない場合

本ワークフローは設定ファイルを `.roo` という隠しフォルダに格納します。表示方法は以下を参照してください。

### macOS (Finder)
* **`Command` + `Shift` + `.` (ドット)** で表示/非表示の切り替え。

### Windows (エクスプローラー)
1. **[表示]** タブ → **[隠しファイル]** をオンにしてください。

### VS Code
```json
"files.exclude": {
    "**/.roo": false
}
```

## License

MIT License
