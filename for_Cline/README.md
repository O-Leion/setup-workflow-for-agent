# setup-workflow-4-Cline

🌐 [English Version](./README_EN.md)

**AI Agent Workspace Scaffolder for Cline**

Cline（VS Code AI拡張機能）において、AIエージェント開発に最適化されたワークスペース環境を自動構築するためのワークフローです。
`.clinerules/` にルール定義、`.clineignore` にAI除外設定を集約した構成を、対話形式で安全にセットアップします。

## 🚀 特徴 (Features)

* **対話型セットアップ**: AIが現状を確認し、ユーザーの意図を汲みながらセットアップを進めます。
* **ルール管理**: `.clinerules/` フォルダにプロジェクトルールを番号付きで整理。
* **AI除外設定**: `.clineignore` で不要なファイルをAIの解析対象から除外。
* **ディレクトリ構造化**: `docs`, `src`, `tests` の分離を自動化。

## 📦 インストール方法 (Installation)

### 1. ディレクトリの確認
お使いのOSに合わせて、以下のディレクトリを開いてください。もしフォルダが存在しない場合は作成してください。

> ⚠️ Cline自体にはGlobal Workflowsの仕組みはありません。このワークフローファイルを新規プロジェクトのルートに配置し、ClineのAIに読み込ませて使用してください。

### 2. 使い方
1. 新規プロジェクト（空のフォルダ）をVS Codeで開きます。
2. `setup-assistant.md` をプロジェクトルートに配置します。
3. Clineのチャットで以下のように指示してください。

> **「`setup-assistant.md` を読んでセットアップを実行してください」**

4. セットアップ完了後、`setup-assistant.md` は削除して構いません。

## 📂 生成される構成 (Directory Structure)

```text
.
├── .clinerules/          <-- [AIルール設定]
│   ├── 01-coding-standards.md
│   └── 02-project-conventions.md
├── .clineignore          <-- [AI除外設定]
├── docs/                 <-- [ドキュメント]
│   └── design/
├── src/                  <-- [ソースコード]
├── tests/                <-- [テストコード]
├── scratchpad.md         <-- [AI作業ログ]
└── .gitignore            <-- [Git設定]
```

## License

MIT License
